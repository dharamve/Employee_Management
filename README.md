# Employee_Management
Employee Management System is a Python-based desktop application using Tkinter and MySQL. It allows users to add, view, update, and delete employee records. The project demonstrates GUI development, database connectivity, SQL queries, and CRUD operations for efficient employee data management.
